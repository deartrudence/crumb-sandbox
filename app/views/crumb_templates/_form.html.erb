<%= form_with(model: crumb_template, local: true) do |form| %>
  <% if crumb_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(crumb_template.errors.count, "error") %> prohibited this crumb_template from being saved:</h2>

      <ul>
      <% crumb_template.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :crumb_template_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
